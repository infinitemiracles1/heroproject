import React from 'react';
import CreateTools from './connect/CreateTools';

/**
 * This component was likely part of a refactor and was left empty.
 * It is being replaced by the CreateTools component, which is now part of the HeroConnectTab.
 * This file has been populated to prevent the application from crashing due to an invalid module import.
 */
const CreateTab: React.FC = () => {
    return <CreateTools />;
};

export default CreateTab;
